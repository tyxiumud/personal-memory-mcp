---
description: 跨客户端 Personal Memory 读取、查询与写入纪律
alwaysApply: true
enabled: true
---

# Personal Memory

## 读取

新会话开始处理有持续背景的任务时，先调用 personal_memory 的 memory_context。用户提到“之前、
还记得、继续、已有方案、我的偏好”等历史背景时，回答前先检索。个人资料使用 global；项目资料
使用 project 和用户约定的稳定 scope_id；领域资料使用 domain；项目或领域读取时包含 global。
不知道项目标识时不要自己创造，先读 global 再结合项目文档确认。同一会话已取得且未变化的背景
可以复用；话题切换、用户纠正或工具报告修订时重新检索。简单翻译、计算等无历史依赖的请求可以
跳过。MCP 不可用时正常处理独立工作，需要历史才能回答的部分明确说明无法核实。

启动读取建议：`view="compact"`、`max_chars=6000`、`limit=8`。注入规则只是触发读取要求，
不等于已经调用 MCP；只有工具真的返回内容才算读到。

## 查询

自然语言整句常常命中不了。用 `query_variants` 补充 2-5 个来自问题和已知背景的关键词，例如
query=`职业`、query_variants=`["数字 IC","工作"]`；query=`投资`、query_variants=`["稳健","红利"]`。
后端对每路执行严格 FTS 查询并按 `score = Σ 1/(60+rank)` 合并；这不是向量语义检索，variant 必须
真的出现在记录里。关键词不能编造用户的职业或持仓；无结果时说明没有找到依据，不要把空 query
的全局浏览当成命中。compact 视图只精简元数据，正文不截断；需要完整证据用 memory_history。

## 写入

用户明确说“记住、记录、以后按这个、纠正、忘记”时，必须立即使用 personal_memory 工具处理。
普通聊天中如出现新确认、长期有用的偏好、个人事实、决定、里程碑、阻塞或稳定下一步，
无需等待触发词，由模型判断并自动写入；没有耐久信息时不要写。

一条记忆尽量只表达一件可独立变化的事：稳定偏好、当前任职、基金配置、关注名单分开保存；
关注标的不等于持仓，计划不等于已完成；历史事件保留时间背景；助手推断不能当成用户已确认事实；
confidence 是来源与写入者的判断。

写前在目标 scope 搜索。一次有意义的轮次通常最多写 1-3 条原子记忆；变化事实用 supersedes，
纠错先查 history 并使用 expected_revision。精确重复直接跳过，不要用 supersedes 去重。
source.client=workbuddy，source.trigger 标记 explicit 或 autonomous，并尽量写 evidence/会话引用。
禁止保存秘密、凭据、推测、原始聊天/工具日志、临时细节或能从仓库轻易重读的代码事实。
只有工具成功返回后才能声称已写入；发现互相矛盾的记录时展示来源与时间，不要只按更新时间覆盖。
